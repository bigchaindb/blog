<nav class="menu menu--main">
    <div class="row row--wide">

        <div class="grid grid--full grid-medium--columns grid--center">
            <div class="grid__col grid__col--1">
                <a class="menu__logo" href="{{@blog.url}}">
                    <svg class="logo logo--sm logo--white--green" aria-labelledby="title">
                        <title>BigchainDB</title>
                        <use xlink:href="{{asset "img/sprite.svg"}}#logo"></use>
                    </svg>
                </a>
            </div>

            <div class="grid__col grid__col--5">

                <div class="menu-overflow">
                    {{#foreach navigation}}

                        <a class="menu__link nav-{{slug}} {{#if current}}active{{/if}} js-tracking-menu" href="{{url absolute="true"}}">{{label}}</a>

                    {{/foreach}}
                </div>

            </div>
        </div>
    </div>
</nav>
